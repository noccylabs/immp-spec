# Input file (markdown)
IN=immp-draft.md
# Output file (pdf)
OUT=immp-draft.pdf
# MSC-files (will be converted to PDF)
IMGMSC=images/auth-cookie.msc images/auth-secret.msc images/pipelines.msc
# DOT-files (will be converted to PDF)
IMGDOT=images/immp-delivery.dot

include Makefile.pd
